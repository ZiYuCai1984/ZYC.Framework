<page:PageBase x:Class="ZYC.Framework.Modules.About.UI.AboutView"
               x:ClassModifier="internal"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:page="clr-namespace:ZYC.Framework.Core.Page;assembly=ZYC.Framework.Core"
               xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
               xmlns:converters="clr-namespace:ZYC.Framework.Core.Converters;assembly=ZYC.Framework.Core"
               xmlns:bindings="clr-namespace:ZYC.Framework.Core.Bindings;assembly=ZYC.Framework.Core"
               mc:Ignorable="d"
               d:DesignHeight="450" d:DesignWidth="800"
               Title="{localizations:L About}">
    <StackPanel x:Name="StackPanel"
                Margin="0 8 0 0">
        <DockPanel Margin="0 0 0 8"
                   Visibility="{Binding Path=IsSelfAlternate,Converter={converters:BoolToVisibilityConverter}}">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L Alternate}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock Text="*"
                       HorizontalAlignment="Right" />
        </DockPanel>
        <DockPanel Margin="0 0 0 8">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L PackageId}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock Text="{Binding CurrentProduct.PackageId}"
                       HorizontalAlignment="Right" />
        </DockPanel>
        <DockPanel Margin="0 0 0 8">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L Version}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock HorizontalAlignment="Right"
                       Text="{Binding CurrentProduct.Version}" />
        </DockPanel>
        <DockPanel Margin="0 0 0 8">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L Author}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock Text="{bindings:LBinding CurrentProduct.Author}"
                       HorizontalAlignment="Right" />
        </DockPanel>
        <DockPanel Margin="0 0 0 8">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L Description}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock Text="{Binding  CurrentProduct.Description}"
                       HorizontalAlignment="Right"
                       TextWrapping="Wrap" />
        </DockPanel>
        <DockPanel Margin="0 0 0 8">
            <TextBlock DockPanel.Dock="Left">
                <TextBlock.Inlines>
                    <Run Text="{localizations:L Copyright}" />
                    <Run Text=" :" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock Text="{Binding CurrentProduct.Copyright}"
                       HorizontalAlignment="Right"
                       TextWrapping="Wrap" />
        </DockPanel>
    </StackPanel>

</page:PageBase>