<UserControl x:Class="ZYC.Framework.Modules.Update.UI.DownloadingView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0 64 0 0">
            <TextBlock Text="{Binding DownloadStatusText,Mode=OneWay}" />
            <Grid Margin="0 16 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ProgressBar Maximum="1"
                             Value="{Binding DownloadProgress,Mode=OneWay}"
                             Height="32" />
                <TextBlock Grid.Row="1"
                           Margin="0 8 0 0"
                           HorizontalAlignment="Right"
                           Text="{Binding DownloadProgress,Mode=OneWay,StringFormat={}{0:P1}}" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
