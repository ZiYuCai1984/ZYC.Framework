<UserControl x:Class="ZYC.Framework.Modules.Update.UI.UpToDateView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:buttons="clr-namespace:ZYC.Framework.Core.Buttons;assembly=ZYC.Framework.Core"
             xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0 64 0 0">
            <TextBlock Text="{localizations:L Already the latest version !!}" />
        </StackPanel>
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,16,0,0"
                Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <buttons:IconButton Text="{localizations:L Clear nuget http cache}"
                                    Kind="DeleteOutline"
                                    Margin="0 0 16 0"
                                    Command="{Binding ClearNuGetHttpCacheCommand}" />
                <buttons:IconButton Text="{localizations:L Check update}"
                                    Kind="Autorenew"
                                    Command="{Binding CheckUpdateCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>