<UserControl x:Class="ZYC.Framework.Modules.Update.UI.Faulted.DownloadFaultedView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:buttons="clr-namespace:ZYC.Framework.Core.Buttons;assembly=ZYC.Framework.Core"
             xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
             mc:Ignorable="d"
             Tag="{Binding UpdateException}"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnDownloadFaultedViewLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="0 64 0 0"
                    Background="Transparent">
            <TextBlock Text="{localizations:L Download failed .}" />
            <ProgressBar Value="0"
                         Height="32"
                         Margin="0 16 0 0" />

            <Border BorderThickness="0"
                    Margin="0 32 0 0"
                    Padding="8"
                    x:Name="Border" />
        </StackPanel>
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <buttons:IconButton Kind="DownloadOutline"
                                    Text="{localizations:L Download}"
                                    Command="{Binding DownloadCommand}"
                                    CommandParameter="{Binding NewProduct}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>