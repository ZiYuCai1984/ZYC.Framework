<notification:NotificationBase x:Class="ZYC.Framework.Modules.Update.UI.Banner.PromptNewProductBanner"
                               x:ClassModifier="internal"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
                               xmlns:buttons="clr-namespace:ZYC.Framework.Core.Buttons;assembly=ZYC.Framework.Core"
                               xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                               xmlns:notification="clr-namespace:ZYC.Framework.Core.Notification;assembly=ZYC.Framework.Core"
                               mc:Ignorable="d"
                               d:DesignHeight="450" d:DesignWidth="800"
                               Background="White">
    <Border Background="{DynamicResource MahApps.Brushes.Validation2}">
        <Grid VerticalAlignment="Center"
              Margin="16 0 8 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                VerticalAlignment="Center">
                <Run Text="{localizations:L New version available}" />
                <Run Text=" -> " />
                <Run Text="{Binding NewProduct.Version,Mode=OneWay}" />
            </TextBlock>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <buttons:IconButton Text="{localizations:L Go to update page}"
                                    Kind="Update"
                                    Margin="0,0,8,0"
                                    Command="{Binding NavigateCommand}" />
                <Button Grid.Column="1"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Command="{Binding CloseNotificationCommand}">
                    <iconPacks:PackIconMaterial Kind="Close"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Width="12"
                                                Height="12" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</notification:NotificationBase>