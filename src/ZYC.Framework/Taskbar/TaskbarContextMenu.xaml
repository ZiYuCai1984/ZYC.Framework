<ContextMenu x:Class="ZYC.Framework.Taskbar.TaskbarContextMenu"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ZYC.Framework.Core.Converters;assembly=ZYC.Framework.Core"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             ItemsSource="{Binding TaskbarMenuItems}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <!--//TODO Style trouble-->
    <!--Style="{DynamicResource MahApps.Styles.ContextMenu}"-->
    <ContextMenu.ItemContainerStyle>
        <Style BasedOn="{StaticResource {x:Type MenuItem}}"
               TargetType="MenuItem">
            <Setter Property="ItemsSource" Value="{Binding SubItems}" />
            <Setter Property="Command"
                    Value="{Binding Command}" />
            <Setter Property="Header">
                <Setter.Value>
                    <MultiBinding Converter="{converters:LocalizationMultiConverter}">
                        <Binding Path="Info.Title" />
                        <Binding Path="Info.Localization" />
                    </MultiBinding>
                </Setter.Value>
            </Setter>
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="False" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="MenuItem">
                                <Separator />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ContextMenu.ItemContainerStyle>
</ContextMenu>