<UserControl x:Class="ZYC.Framework.MainWindowView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:ZYC.Framework.Core;assembly=ZYC.Framework.Core"
             xmlns:workspace="clr-namespace:ZYC.Framework.Workspace"
             xmlns:statusBar="clr-namespace:ZYC.Framework.StatusBar"
             xmlns:windowTitle="clr-namespace:ZYC.Framework.WindowTitle"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnMainWindowViewLoaded"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <AdornerDecorator x:Name="AdornerDecorator">
        <Grid x:Name="RootGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <core:Component Type="windowTitle:WindowTitleView" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1">
                    <core:Component Type="workspace:ParallelWorkspaceView" />
                </Grid>
            </Grid>


            <Grid Grid.Row="2">
                <core:Component Type="statusBar:StatusBarView" />
            </Grid>
        </Grid>
    </AdornerDecorator>
</UserControl>