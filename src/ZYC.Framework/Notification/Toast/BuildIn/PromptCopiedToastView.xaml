<toast:ToastBase x:Class="ZYC.Framework.Notification.Toast.BuildIn.PromptCopiedToastView"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:toast="clr-namespace:ZYC.Framework.Core.Notification.Toast;assembly=ZYC.Framework.Core"
                 mc:Ignorable="d"
                 d:DesignHeight="450" d:DesignWidth="800">
    <!--!WARNING Design by chatgpt 5.2 -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <iconPacks:PackIconMaterial Grid.Column="0"
                                    Kind="ClipboardCheckOutline"
                                    Width="18"
                                    Height="18"
                                    VerticalAlignment="Center"
                                    Opacity="0.9" />
        <TextBlock Grid.Column="2"
                   Text="{localizations:L Copied to clipboard.}"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Opacity="0.95" />
        <Button Grid.Column="3"
                Width="28"
                Height="28"
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                Command="{Binding CloseBannerCommand}"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            CornerRadius="999"
                            Background="Transparent">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource MahApps.Brushes.Gray10}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource MahApps.Brushes.Gray20}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
            <iconPacks:PackIconMaterial Kind="Close"
                                        Width="14"
                                        Height="14"
                                        Opacity="0.85" />
        </Button>
    </Grid>
</toast:ToastBase>