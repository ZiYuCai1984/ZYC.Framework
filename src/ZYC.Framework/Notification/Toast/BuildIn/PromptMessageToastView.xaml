<toast:ToastBase x:Class="ZYC.Framework.Notification.Toast.BuildIn.PromptMessageToastView"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:toast="clr-namespace:ZYC.Framework.Core.Notification.Toast;assembly=ZYC.Framework.Core"
                 xmlns:core="clr-namespace:ZYC.Framework.Core;assembly=ZYC.Framework.Core"
                 xmlns:converters="clr-namespace:ZYC.Framework.Core.Converters;assembly=ZYC.Framework.Core"
                 mc:Ignorable="d"
                 MaxHeight="256"
                 d:DesignHeight="450" d:DesignWidth="800">
    <Grid
        DataContext="{Binding ToastMessage}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <core:HybridIcon
            Icon="{Binding Icon}"
            Width="18"
            Height="18"
            VerticalAlignment="Top"
            Margin="0 2 0 0"
            Opacity="0.9" />
        <TextBlock Grid.Column="2"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"
                   TextTrimming="CharacterEllipsis"
                   FontSize="13"
                   Opacity="0.95">
            <TextBlock.Text>
                <MultiBinding Converter="{converters:LocalizationMultiConverter}">
                    <Binding Path="Text" />
                    <Binding Path="Localization" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
    </Grid>
</toast:ToastBase>