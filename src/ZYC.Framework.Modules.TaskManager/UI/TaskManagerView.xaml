<page:PageBase x:Class="ZYC.Framework.Modules.TaskManager.UI.TaskManagerView"
               x:ClassModifier="internal"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:page="clr-namespace:ZYC.Framework.Core.Page;assembly=ZYC.Framework.Core"
               xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
               xmlns:bindings="clr-namespace:ZYC.Framework.Core.Bindings;assembly=ZYC.Framework.Core"
               xmlns:converters="clr-namespace:ZYC.Framework.Core.Converters;assembly=ZYC.Framework.Core"
               xmlns:wpf="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
               xmlns:markupEx="clr-namespace:ZYC.Framework.Core.MarkupEx;assembly=ZYC.Framework.Core"
               xmlns:core="clr-namespace:ZYC.Framework.Core;assembly=ZYC.Framework.Core"
               xmlns:local="clr-namespace:ZYC.Framework.Modules.TaskManager.Abstractions;assembly=ZYC.Framework.Modules.TaskManager.Abstractions"
               xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
               xmlns:buttons="clr-namespace:ZYC.Framework.Core.Buttons;assembly=ZYC.Framework.Core"
               xmlns:converters1="clr-namespace:ZYC.Framework.Modules.TaskManager.Converters"
               xmlns:commands="clr-namespace:ZYC.Framework.Modules.TaskManager.Commands"
               mc:Ignorable="d"
               d:DesignHeight="450" d:DesignWidth="800"
               Loaded="OnTaskManagerViewLoaded"
               Title="{localizations:L Task Manager}"
               DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <page:PageBase.HeaderAdditionContent>
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <ComboBox Width="128"
                      ItemsSource="{markupEx:EnumBindingSource {x:Type local:FilterType}}"
                      Margin="16 0 0 0"
                      Height="24"
                      SelectedItem="{Binding FilterType,Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <wpf:TextBlock Text="{bindings:LBinding .}"
                                       Margin="8 0 0 0"
                                       VerticalAlignment="Center" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBox Text="{Binding FilterText,UpdateSourceTrigger=PropertyChanged}"
                     mah:TextBoxHelper.Watermark="{localizations:L Search task}"
                     Width="256"
                     Height="24"
                     Margin="16 0 0 0"
                     Style="{DynamicResource MahApps.Styles.TextBox.Search}" />

        </StackPanel>
    </page:PageBase.HeaderAdditionContent>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 8 0 0">
            <buttons:IconButton
                Kind="TrashCanOutline"
                Text="{localizations:L Clean up tasks}"
                Command="{markupEx:LifetimeScopeResolve commands:ClearUpTasksCommand}" />
        </StackPanel>
        <ScrollViewer Grid.Row="1"
                      Margin="0 8 0 0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      SnapsToDevicePixels="True">
            <ItemsControl ItemsSource="{Binding ManagedTasksCollectionView}">
                <ItemsControl.Resources>
                    <Style x:Key="StatusStripStyle" TargetType="Border">
                        <Setter Property="Background"
                                Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Running}">
                                <Setter Property="Background" Value="#2FA36B" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Paused}">
                                <Setter Property="Background" Value="#D99028" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Faulted}">
                                <Setter Property="Background" Value="#C0392B" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Canceled}">
                                <Setter Property="Background" Value="#8A8A8A" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Completed}">
                                <Setter Property="Background" Value="#4CAF72" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="StateBadgeStyle" TargetType="Border">
                        <Setter Property="Padding" Value="7 2" />
                        <Setter Property="CornerRadius" Value="9" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Background"
                                Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" />
                        <Setter Property="BorderBrush"
                                Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Running}">
                                <Setter Property="Background" Value="#E6F6EE" />
                                <Setter Property="BorderBrush" Value="#7ACB9A" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Paused}">
                                <Setter Property="Background" Value="#FFF4E5" />
                                <Setter Property="BorderBrush" Value="#F0B35A" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Faulted}">
                                <Setter Property="Background" Value="#FDEAEA" />
                                <Setter Property="BorderBrush" Value="#E07A7A" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Canceled}">
                                <Setter Property="Background" Value="#F1F1F1" />
                                <Setter Property="BorderBrush" Value="#BDBDBD" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding State}"
                                         Value="{x:Static local:ManagedTaskState.Completed}">
                                <Setter Property="Background" Value="#E8F7EF" />
                                <Setter Property="BorderBrush" Value="#4CAF72" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="MetaTextStyle" TargetType="TextBlock">
                        <Setter Property="FontSize" Value="11" />
                        <Setter Property="Opacity" Value="0.72" />
                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>

                    <Style x:Key="ActionButtonStyle" TargetType="Button">
                        <Setter Property="Width" Value="30" />
                        <Setter Property="Height" Value="30" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0 0 4 0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ItemsControl.Resources>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border MinHeight="68"
                                BorderThickness="0 0 0 1"
                                Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4" />
                                    <ColumnDefinition Width="2.1*" />
                                    <ColumnDefinition Width="1.4*" />
                                    <ColumnDefinition Width="156" />
                                    <ColumnDefinition Width="112" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                        Style="{StaticResource StatusStripStyle}" />

                                <Grid Grid.Column="1"
                                      Margin="12 8 12 8"
                                      VerticalAlignment="Center">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock FontSize="13"
                                               FontWeight="SemiBold"
                                               Text="{Binding DefinitionId}"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" />

                                    <TextBlock Grid.Row="1"
                                               Grid.Column="0"
                                               Margin="0 5 0 0"
                                               Style="{StaticResource MetaTextStyle}">
                                        <Run Text="{Binding ProviderId,Mode=OneWay}" />
                                        <Run Text=" / v" />
                                        <Run Text="{Binding DefinitionVersion,Mode=OneWay}" />
                                    </TextBlock>

                                    <Border Grid.Row="0"
                                            Grid.RowSpan="2"
                                            Grid.Column="1"
                                            Margin="8 0 0 0"
                                            VerticalAlignment="Center"
                                            Style="{StaticResource StateBadgeStyle}">
                                        <TextBlock FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding State}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </Grid>

                                <Grid Grid.Column="2"
                                      Margin="0 8 12 8"
                                      VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusText}"
                                               TextTrimming="CharacterEllipsis"
                                               Opacity="0.86" />
                                    <TextBlock Text="{Binding FaultText}"
                                               TextTrimming="CharacterEllipsis"
                                               Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                               Foreground="#C0392B">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding FaultText}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding FaultText}" Value="">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>

                                <Grid Grid.Column="3"
                                      Margin="0 8 16 8"
                                      VerticalAlignment="Center">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ProgressBar Height="7"
                                                 Maximum="1"
                                                 VerticalAlignment="Center">
                                        <ProgressBar.Value>
                                            <MultiBinding Converter="{converters1:ProgressMultiValueConverter}">
                                                <Binding Path="Progress" Mode="OneWay" />
                                                <Binding Path="State" Mode="OneWay" />
                                            </MultiBinding>
                                        </ProgressBar.Value>
                                    </ProgressBar>
                                    <TextBlock Grid.Row="1"
                                               Margin="0 5 0 0"
                                               HorizontalAlignment="Right"
                                               Style="{StaticResource MetaTextStyle}">
                                        <TextBlock.Text>
                                            <MultiBinding Converter="{converters1:ProgressMultiValueConverter}"
                                                          StringFormat="{}{0:P1}">
                                                <Binding Path="Progress" Mode="OneWay" />
                                                <Binding Path="State" Mode="OneWay" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </Grid>

                                <Grid Grid.Column="4"
                                      Margin="0 8 12 8"
                                      VerticalAlignment="Center">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:MM-dd HH:mm}}"
                                               Style="{StaticResource MetaTextStyle}" />
                                    <TextBlock Grid.Row="1"
                                               Margin="0 5 0 0"
                                               Text="{Binding PauseKind}"
                                               Style="{StaticResource MetaTextStyle}" />
                                </Grid>

                                <StackPanel Grid.Column="5"
                                            Margin="0 0 8 0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                    <Button Style="{StaticResource ActionButtonStyle}"
                                            Command="{markupEx:LifetimeScopeResolve commands:ResumeCommand}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="Resume">
                                        <core:HybridIcon Width="17" Height="17" Icon="PlayCircleOutline" />
                                    </Button>

                                    <Button Style="{StaticResource ActionButtonStyle}"
                                            Command="{markupEx:LifetimeScopeResolve commands:PauseCommand}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="Pause">
                                        <core:HybridIcon Width="17" Height="17" Icon="PauseCircleOutline" />
                                    </Button>

                                    <Button Style="{StaticResource ActionButtonStyle}"
                                            Command="{markupEx:LifetimeScopeResolve commands:CancelCommand}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="Cancel"
                                            Margin="0">
                                        <core:HybridIcon Width="17" Height="17" Icon="CloseCircleOutline" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <!--!WARNING Binding ElementName directly to ItemControl's Items.Count seems reasonable here. However, on the first launch, when loading in the background, the binding will fail due to the visual tree loading order.-->
        <page:NoItemView Grid.Row="1"
                         Visibility="{Binding CollectionViewCount,Converter={converters:IntToVisibilityConverter Reverse=True},FallbackValue=Collapsed}" />
    </Grid>
</page:PageBase>
