<UserControl x:Class="ZYC.Framework.Modules.Mock.UI.TestCLIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:commands="clr-namespace:ZYC.Framework.Core.Commands;assembly=ZYC.Framework.Core"
             xmlns:markupEx="clr-namespace:ZYC.Framework.Core.MarkupEx;assembly=ZYC.Framework.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MockResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="CLI" />
            <Button Command="{markupEx:LifetimeScopeResolve commands:NavigateCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self},Path=Content}"
                    Content="zyc://cli?&amp;type=1&amp;text=git%20status" />
            <Button Command="{markupEx:LifetimeScopeResolve commands:NavigateCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self},Path=Content}"
                    Content="zyc://cli/?exec=timeout%20/t%203%20/nobreak&amp;exec=dir" />
            <Button Command="{markupEx:LifetimeScopeResolve commands:NavigateCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self},Path=Content}"
                    Content="zyc://cli/?exec=timeout%20/t%203%20/nobreak&amp;exec=dir&amp;exec=whoami" />
            <Button Content="Navigate 50 times"
                    Click="OnNavigate50TimesButtonClick"/>
        </StackPanel>
    </ScrollViewer>

</UserControl>
