<UserControl x:Class="ZYC.Framework.Modules.Mock.UI.TestTaskManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:markupEx="clr-namespace:ZYC.Framework.Core.MarkupEx;assembly=ZYC.Framework.Core"
             xmlns:update="clr-namespace:ZYC.Framework.Modules.Update.Abstractions.Commands;assembly=ZYC.Framework.Modules.Update.Abstractions"
             xmlns:commands="clr-namespace:ZYC.Framework.Modules.Mock.Commands"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MockResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Create count task" />
            <ItemsControl ItemsSource="{StaticResource NumItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{markupEx:LifetimeScopeResolve commands:CountTaskCommand}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                CommandParameter="{Binding .}">
                            <TextBlock Text="{Binding }"
                                       Margin="0" />
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="Update" />
            <Button Content="Check update"
                    Command="{markupEx:LifetimeScopeResolve update:ICheckUpdateCommand}" />

        </StackPanel>
    </ScrollViewer>
</UserControl>