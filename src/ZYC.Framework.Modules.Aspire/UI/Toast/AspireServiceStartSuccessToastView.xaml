<toast:ToastBase x:Class="ZYC.Framework.Modules.Aspire.UI.Toast.AspireServiceStartSuccessToastView"
                 x:ClassModifier="internal"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:markupEx="clr-namespace:ZYC.Framework.Core.MarkupEx;assembly=ZYC.Framework.Core"
                 xmlns:commands="clr-namespace:ZYC.Framework.Core.Commands;assembly=ZYC.Framework.Core"
                 xmlns:toast="clr-namespace:ZYC.Framework.Core.Notification.Toast;assembly=ZYC.Framework.Core"
                 xmlns:core="clr-namespace:ZYC.Framework.Core;assembly=ZYC.Framework.Core"
                 xmlns:localizations="clr-namespace:ZYC.Framework.Core.Localizations;assembly=ZYC.Framework.Core"
                 xmlns:abstractions="clr-namespace:ZYC.Framework.Abstractions;assembly=ZYC.Framework.Abstractions"
                 xmlns:buttons="clr-namespace:ZYC.Framework.Core.Buttons;assembly=ZYC.Framework.Core"
                 mc:Ignorable="d"
                 d:DesignHeight="450" d:DesignWidth="800"
                 DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <core:HybridIcon
                Icon="{x:Static abstractions:Base64IconResources.Aspire}"
                Width="18" Height="18"
                VerticalAlignment="Center"
                Opacity="0.9"
                VerticalContentAlignment="Top" />
            <TextBlock Grid.Column="1"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Opacity="0.95"
                       Text="{localizations:L Aspire is ready}"
                       Margin="8 0 0 0" />
        </Grid>

        <buttons:IconButton Grid.Row="1"
                            Kind="KeyboardReturn"
                            Text="{localizations:L Go to dashboard}"
                            Command="{markupEx:LifetimeScopeResolve commands:NavigateCommand}"
                            CommandParameter="{Binding TargetUri}"
                            HorizontalAlignment="Right" />

    </Grid>
</toast:ToastBase>