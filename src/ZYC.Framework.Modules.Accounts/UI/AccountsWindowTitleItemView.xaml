<UserControl x:Class="ZYC.Framework.Modules.Accounts.UI.AccountsWindowTitleItemView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:ZYC.Framework.Core;assembly=ZYC.Framework.Core"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:ZYC.Framework.Core.Converters;assembly=ZYC.Framework.Core"
             mc:Ignorable="d"
             d:DesignHeight="32"
             d:DesignWidth="180"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ZYC.Framework;component/Styles/BottomRightDropDownButton.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <mah:DropDownButton
        Background="Transparent"
        BorderBrush="Transparent"
        ItemsSource="{Binding MainMenuItems}">
        <mah:DropDownButton.Content>
            <StackPanel Orientation="Horizontal">
                <core:HybridIcon HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 Width="16"
                                 Height="16"
                                 Icon="{Binding Icon}">
                </core:HybridIcon>
                <TextBlock Text="{Binding DisplayText}"
                           FontSize="12"
                           Margin="8 0 4 0"
                           FontWeight="SemiBold" />
            </StackPanel>
        </mah:DropDownButton.Content>
        <mah:DropDownButton.ItemContainerStyle>
            <Style BasedOn="{StaticResource MahApps.Styles.MenuItem}"
                   TargetType="{x:Type MenuItem}">
                <Setter Property="ItemsSource" Value="{Binding SubItems}" />
                <Setter Property="Icon"
                        Value="{Binding Icon}" />
                <Setter Property="Command" Value="{Binding  Command}" />
                <Setter Property="Header">
                    <Setter.Value>
                        <MultiBinding Converter="{converters:LocalizationMultiConverter}">
                            <Binding Path="Title" />
                            <Binding Path="Localization" />
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
            </Style>
        </mah:DropDownButton.ItemContainerStyle>
    </mah:DropDownButton>
</UserControl>
