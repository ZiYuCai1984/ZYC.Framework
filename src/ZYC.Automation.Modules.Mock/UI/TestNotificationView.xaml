<UserControl x:Class="ZYC.Automation.Modules.Mock.UI.TestNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MockResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Banner" />
            <Button Click="OnPromptRestartBtnClick"
                    Content="Restart Banner" />
            <TextBlock Text="Toast" />
            <Button Click="OnPromptToastInfoBtnClick"
                    Content="Info" />
            <Button Click="OnPromptToastLongInfoBtnClick"
                    Content="Long Info" />
            <Button Click="OnPromptToastWarnBtnClick"
                    Content="Warn" />
            <Button Click="OnPromptToastErrorBtnClick"
                    Content="Error" />
            <Button Click="OnDelayPromptToastBtnClick"
                    Content="Delay" />
        </StackPanel>
    </ScrollViewer>

</UserControl>