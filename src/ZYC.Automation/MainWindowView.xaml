<UserControl x:Class="ZYC.Automation.MainWindowView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:ZYC.Automation.Core;assembly=ZYC.Automation.Core"
             xmlns:windowTitle="clr-namespace:ZYC.Automation.WindowTitle"
             xmlns:workspace="clr-namespace:ZYC.Automation.Workspace"
             xmlns:statusBar="clr-namespace:ZYC.Automation.StatusBar"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnMainWindowViewLoaded"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <AdornerDecorator>
        <Grid x:Name="RootGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <core:Component Type="windowTitle:WindowTitleView" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <!--<core:Component Type="sideBar:SideBarView" />-->

                <Grid Grid.Column="1">
                    <core:Component Type="workspace:ParallelWorkspaceView" />
                </Grid>
            </Grid>


            <Grid Grid.Row="2">
                <core:Component Type="statusBar:StatusBarView" />
            </Grid>
        </Grid>
    </AdornerDecorator>
</UserControl>